# deleteStream
**DEL**  
**Required fields:** `stream_id`  
`deleteStream` deletes a `stream` configuration record.
  * Example `deleteStream` call.  
    ```
    {
      "stream_id": "[stream_id]"
    }
    ```
