# getStream
**POST**  
**Required fields:** `stream_id`  
`getStream` gets the `stream` configuration with the supplied `stream_id`.
  * Example `getStream` call.  
    ```
    {
      "stream_id": "[stream_id]"
    }
    ```
