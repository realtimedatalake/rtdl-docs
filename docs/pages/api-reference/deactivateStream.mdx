# deactivateStream
**PUT**  
**Required fields:** `stream_id`  
`deactivateStream` setes the `active` value in a `stream` configuration record to false.
  * Example `activateStream` call.  
    ```
    {
      "stream_id": "[stream_id]"
    }
    ```
